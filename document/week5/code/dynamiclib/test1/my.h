#include <stdio.h>
#include <stdlib.h>
#include <sys/time.h>
#include <time.h>
#define MAXN 10
#define MAXSIZE 5

void randinit(int*, int);
int max(int*, int);

int sum(int*, int);
void show(int*, int);
